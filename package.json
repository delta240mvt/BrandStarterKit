{
  "name": "polish-service-website-starter",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host",
    "test": "node --import tsx --test src/data/starter.test.ts src/lib/import-boundary.test.ts src/lib/standalone-seo.test.ts src/lib/seo.test.ts src/lib/schema.test.ts src/lib/discovery.test.ts",
    "build": "astro build",
    "start": "astro dev --host",
    "preview": "node --input-type=module -e \"import { spawn } from 'node:child_process'; const forwarded = []; for (let i = 1; i < process.argv.length; i += 1) { const arg = process.argv[i]; if (arg === '--host') { forwarded.push('--ip'); continue; } if (arg.startsWith('--host=')) { forwarded.push(`--ip=${arg.slice('--host='.length)}`); continue; } forwarded.push(arg); } const child = spawn('wrangler', ['pages', 'dev', './dist', ...forwarded], { stdio: 'inherit', shell: process.platform === 'win32' }); child.on('exit', (code, signal) => { if (signal) { console.error(`preview terminated by signal ${signal}`); process.exit(1); } process.exit(code ?? 1); }); child.on('error', (error) => { console.error(error); process.exit(1); });\" --",
    "deploy": "wrangler pages deploy ./dist",
    "check": "npm run test && astro check",
    "test:ui": "node scripts/verify-refresh.mjs"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.3",
    "@hyperframes/player": "^0.8.36",
    "astro": "^7.1.3",
    "gsap": "^3.15.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@playwright/test": "^1.61.1",
    "@tailwindcss/typography": "^0.5.20",
    "@types/node": "^26.1.1",
    "playwright": "^1.61.1",
    "tailwindcss": "^3.4.19",
    "tsx": "^4.23.1",
    "typescript": "^5.7.0",
    "wrangler": "^4.114.0"
  }
}
